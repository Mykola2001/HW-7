
let number = prompt("Write some number here if you don't mind");

let resoult;

if (number < 1 > -1) {
    resoult = 0
};
if (number >= 1) {
    resoult = 1
};
if (number < 0) {
    resoult = -1
};

const alertNumber = alert(resoult);